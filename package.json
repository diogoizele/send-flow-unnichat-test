{
  "name": "send-flow-unnichat-test",
  "private": true,
  "workspaces": [
    "functions",
    "web"
  ],
  "scripts": {
    "build:functions": "cd functions && npm run build",
    "build:web": "cd web && npm run build",
    "deploy": "firebase deploy"
  }
}
