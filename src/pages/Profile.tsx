import { Box, Typography } from "@mui/material";

export const Profile = () => (
  <Box p={4} width="100vw">
    <Typography variant="h4">Profile</Typography>
    <Typography mt={2}>Esta é a página de perfil protegida.</Typography>
  </Box>
);
